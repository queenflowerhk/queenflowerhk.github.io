Vanilla HTML/CSS/JavaScript with no build step; Tailwind CSS loaded from CDN with custom gold color palette and serif/sans font extensions; Font Awesome 6 for icons; Google Fonts (Playfair Display, Inter, Noto Serif TC, Noto Sans TC); localStorage for cart persistence; JSON files (`products.json`, `translations.json`) as data sources; deployed on GitHub Pages (`.nojekyll`, `CNAME`).